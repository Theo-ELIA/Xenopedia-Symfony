<form method="post" {{ form_enctype(form) }}>
 {% debug form %}
{{ form_widget(form) }}
<input type="submit" class="btn btn-primary" />
<a href="{{path('xenopedia_admin_grant_admin', { username : form.vars.value.username } )}}">Donner les droits admins</a>
</form>